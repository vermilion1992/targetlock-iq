# TargetLock IQ v2 RC1 — Final automated pilot gate

**Gate date:** 2026-06-05  
**Scope:** `packages/starterkit`, `docs/targetlock-pitch`  
**Purpose:** Automated and code-level confidence check before external pilot testing (no new features).

---

## Verdict

**Ready for pilot**

Build, tests, production route smoke, export paths, copy/docs alignment, and browser smoke all passed. Remaining items are documented non-blocking limitations (local storage, advisory steering/branch, no HUB-IQ API).

---

## Commands run

From `packages/starterkit`:

| Command | Result |
|---------|--------|
| `npm run test` | **Pass** — 213 tests, 37 files |
| `npm run lint` (`tsc --noEmit`) | **Pass** |
| `npm run build` | **Pass** — Next.js 16.2.4 standalone |
| `npm start` + HTTP smoke | **Pass** — see route table |
| `npm run smoke:pilot` | **Pass** — Playwright Chromium |

Final re-verification after gate fixes: same three commands **pass**.

---

## Route smoke (production server)

Server: `npm run build` then `npm start` (`start-railway.mjs`, `0.0.0.0:8080`).

| URL | Status | Notes |
|-----|--------|-------|
| `http://localhost:8080/` | 307 | Redirects to `/targetlock` |
| `http://localhost:8080/targetlock` | 200 | App shell; client hydrates after load |
| `http://localhost:8080/templates/targetlock-plan-template.csv` | 200 | |
| `http://localhost:8080/templates/targetlock-survey-template.csv` | 200 | |
| `http://localhost:8080/templates/targetlock-survey-with-metadata.csv` | 200 | |
| `http://localhost:8080/pitch-handover-sample.pdf` | 200 | Regenerated by vitest sample test |
| `/_next/static/...` (from page HTML) | 200 | Static chunk served |

No 500 responses on first request.

---

## Export smoke

| Path | How verified |
|------|----------------|
| TXT handover | `report.test.ts` — `buildReportText`, filenames, recovery sections |
| PDF handover | `generate-pitch-samples.test.ts` — `buildHandoverPdfBlob`, `%PDF` header, size &gt; 500 bytes; writes `docs/targetlock-pitch/samples/*` and `public/pitch-handover-sample.pdf` |
| Branch report data | `branch-report-data.test.ts` |
| Branch report PDF | **Added** `branch-report-pdf.test.ts` — `buildBranchReportPdfBlob` non-empty PDF |
| Hole package JSON | `hole-package.test.ts` — round-trip with branch + approval |
| Safe filenames | `report.test.ts` — `handoverFilename` sanitization |

---

## Copy consistency

| Pattern | Result |
|---------|--------|
| “Driller instruction” in UI/reports | **None** in `src` (guarded by `action-plan-copy.test.ts`) |
| “Correct now” in UI display | Mapped to **Correction advisable** via `formatRecoveryActionDisplay`; internal status key unchanged |
| “pipe bending”, “use motor now”, “must steer”, “permanent instruction” | **None** in user-facing report blob test |
| “pitch tour” | CSS class names only (`pitch-active`) — not user-visible |
| “Test Lab” | **Fixed** — `synthetic-hole-builder.ts` default name → “Scenario Lab hole” |
| “Planned CSV” / “Actual CSV” in UI | Sidebar uses **Hole plan** / **Survey results**; **fixed** guide branch step and `validation.ts` warning |
| History log label | **Fixed** — “Import survey results” (was “Import actual CSV”) |

**Deferred (non-blocking):** Internal steering action key `Correct now` in `steering-types.ts` / Scenario Lab inspect strings — not shown as rig-floor hero label.

---

## Docs consistency

| Item | Action |
|------|--------|
| [`README.md`](./README.md) | Updated to v2 RC1, 213 tests, pilot gate links, `smoke:pilot` |
| [`hub-iq-import.md`](./hub-iq-import.md) | Import steps → Hole plan / Survey results |
| [`usability-test-runbook.md`](./usability-test-runbook.md) | Observer prompt → “next interval aim” |
| [`release-candidate-checklist.md`](./release-candidate-checklist.md) | Test count 213+; link to this gate doc |
| Removed modules | No references to `GuideModal`, `pitch-scenario`, `use-pitch-mode` in app code |
| Audit cross-links | `rc1-general-audit`, sidebar/non-sidebar audits, known-limitations, pilot-testing-kit — linked from README |

---

## Browser smoke

**Ran:** `npm run smoke:pilot` against `http://localhost:8080/targetlock`

- Wait for hydration (logo alt “TargetLock IQ”)
- Guide Center open/close
- Scenario Lab open/close
- Simple/Advanced toggle
- Export TXT / Export PDF buttons visible
- No console errors (benign favicon/devtools filtered)

**Note:** Initial script expected a `TargetLock IQ` heading (not in DOM — brand is logo + version tag). Smoke script updated to match RC1 UI.

---

## Startup / recovery audit (code review)

| Check | Status |
|-------|--------|
| Corrupt storage → recovery screen (not infinite spinner) | OK — `hydrated` set on corrupt |
| Error boundary + reset all local data | OK — `TargetLockErrorBoundary` |
| Guide exit restores persistence | OK — `exitGuide` → `onActiveChange(false)` |
| Hole package import atomic | OK — parse/confirm before `importHolePackage` |

---

## Fixes made in this gate

| File | Change |
|------|--------|
| `src/lib/drilling/guide-flows.ts` | Survey results wording in branch import step |
| `src/lib/drilling/synthetic-hole-builder.ts` | Default hole name → Scenario Lab hole |
| `src/lib/drilling/validation.ts` | User warning → “hole plan” |
| `src/app/targetlock/TargetLockApp.tsx` | History label for survey import |
| `src/lib/drilling/__tests__/branch-report-pdf.test.ts` | **New** — branch PDF smoke |
| `src/lib/drilling/__tests__/generate-pitch-samples.test.ts` | PDF size/header assertions |
| `scripts/pilot-gate-smoke.mjs` | **New** — Playwright RC1 smoke |
| `package.json` | `smoke:pilot` script |
| `docs/targetlock-pitch/README.md` | RC1 index, counts, gate link |
| `docs/targetlock-pitch/hub-iq-import.md` | In-app import labels |
| `docs/targetlock-pitch/usability-test-runbook.md` | Observer wording |
| `docs/targetlock-pitch/release-candidate-checklist.md` | Gate link + test count |

---

## Remaining non-blocking risks

See [`pilot-testing-kit/known-limitations.md`](./pilot-testing-kit/known-limitations.md):

- Browser `localStorage` only — no multi-user sync
- Steering feasibility and branch kickoff/toolface remain **advisory**
- Internal status key `Correct now` still used in math/tests; UI shows **Correction advisable**
- No live HUB-IQ API in RC1
- Public pilot URL has no authentication

**Manual checks still recommended:** one Standard + one Branch guide run, PDF open on reviewer machine, refresh after branch daughter save ([`release-candidate-checklist.md`](./release-candidate-checklist.md)).

---

## Related

- [RC1 general audit](./rc1-general-audit.md)
- [Copy audit RC1](./copy-audit-rc1.md)
- [Release candidate checklist](./release-candidate-checklist.md)
- [Pilot testing kit](./pilot-testing-kit/guide-center.md)
