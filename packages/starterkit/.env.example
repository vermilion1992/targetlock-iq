# Copy to packages/starterkit/.env.local (NOT the repo root).
# Restart `npm run dev` after changing NEXT_PUBLIC_* variables.

# Optional — enables Mapbox Satellite in Planner → Map → Satellite view
NEXT_PUBLIC_MAPBOX_TOKEN=
