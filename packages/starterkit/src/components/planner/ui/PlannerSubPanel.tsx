"use client";

/**
 * Planner reference sub-panel. Thin alias of the shared dashboard HeroSubPanel
 * so the planner and TargetLock "How it works" views share one implementation.
 */
export { HeroSubPanel as PlannerSubPanel } from "@/components/dashboard/HeroSubPanel";
